# 个人网站使用指南（人工操作版）

> 这篇指南给"人"看：不要求懂代码，照着做就能发文章、改样式。
> 仓库位置：`C:\Users\55871\天堂洗衣机的目录`
> 技术细节见同目录下的 `SITE_GUIDE.md`。

---

## 一、发一篇新文章（3 步）

### 第 1 步：打开终端并进入网站目录

在网站目录下打开 PowerShell（地址栏输入 `powershell` 回车，或先 `cd` 过去）。

### 第 2 步：新建文章文件

```powershell
hexo new "我的第一篇博客"
```

> 如果提示 `hexo` 不是命令，改用：`npx hexo new "我的第一篇博客"`

执行后会在 `source\_posts\` 下生成 `我的第一篇博客.md`，用任何编辑器（记事本/VSCode/Obsidian）打开它，会看到：

```markdown
---
title: 我的第一篇博客
date: 2026-08-31 12:00:00
tags:
---
```

在 `---` 下面写正文（支持 Markdown：`#` 标题、`**加粗**`、图片、代码块等）。

### 第 3 步：本地预览确认效果（可选但推荐）

```powershell
hexo server
```

浏览器打开 http://localhost:4000 就能看到效果。确认没问题后，按 `Ctrl+C` 停止预览。

### 第 4 步：构建并推送到 GitHub

```powershell
hexo generate
git add .
git commit -m "新增文章：我的第一篇博客"
git push
```

> 如果网站接了自动部署（Netlify/Pages），推送后网站会自动更新；没有接的话，推完 GitHub 只是备份。

---

## 二、编辑已有文章

直接用编辑器打开 `source\_posts\` 下对应的 `.md` 文件修改，然后重复"第 4 步"的构建 + 推送即可。

> 💡 修改 `date` 字段可以改变文章发布时间；加 `tags:` 或 `categories:` 字段可以给文章打标签、分类。

---

## 三、修改网站样式（主题）

本网站的样式由 **Butterfly 主题**控制，有两种改法：

### 方式 A：改主题配置（菜单、头像、首页设置等）

主题的"开关面板"是配置文件 `node_modules\hexo-theme-butterfly\_config.yml`（用编辑器打开，里面每一项都有注释说明，比如 `nav` 导航栏、`avatar` 头像、`footer` 页脚）。

改完保存 → 重新 `hexo generate` → 刷新页面看效果。

> ⚠️ 注意：网站根目录有个 `_config.butterfly.yml` 文件，它目前的内容是复制错的（是站点配置，不是主题配置）。**不要往里面乱加东西**；想覆盖主题设置时，在里面写"键名: 值"即可（Hexo 会自动合并，根目录的优先）。

### 方式 B：自定义 CSS（改颜色、字体、布局细节）

想要更自由的样式修改，可以在 `node_modules\hexo-theme-butterfly\source\css\` 下找到主题的样式文件，或者新建一个自定义 CSS 文件并在主题配置里引入。

> 进阶操作建议：在 `source\` 下建一个 `css\custom.css`，然后在主题配置的 `inject` 部分引入它，这样升级主题时不会被覆盖。

### 修改站点标题、作者等

打开 `_config.yml`，改这几项：

```yaml
title: 网站标题
author: 你的名字
language: zh-CN        # 改成中文界面
url: https://你的真实域名
```

改完记得 `hexo generate` 重新生成。

---

## 四、常见问题

| 问题 | 解决办法 |
| ---- | ---- |
| `hexo` 不是内部或外部命令 | 用 `npx hexo ...` 代替 |
| 预览时改了内容没变化 | 预览是实时刷新的；若不行按 Ctrl+C 重新 `hexo server` |
| 改了配置没效果 | 先 `hexo clean` 再 `hexo generate` |
| 端口 4000 被占用 | `hexo server -p 5000` 换个端口 |
| 推送到 GitHub 提示没权限 | 检查 SSH key 是否配置（见 GitHub 设置 → SSH keys） |

---

## 五、还没做的事（代办清单）

- [ ] 修改 `_config.yml` 的 `author` / `url` / `language`
- [ ] 添加 `.gitignore` 并移除已入库的 `public/`、`db.json`（详见 SITE_GUIDE.md §4.2）
- [ ] 删除模板文章 `hello-world.md`、`test.md`，写第一篇正式文章
- [ ] 选择部署方式（Netlify / GitHub Pages / Vercel）并配置
