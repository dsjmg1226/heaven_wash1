# 个人网站使用指南（人工操作版）

> 这篇指南给"人"看：不要求懂代码，照着做就能发文章、改样式。
> 仓库位置：`C:\Users\55871\天堂洗衣机的目录`
> 技术细节见同目录下的 `SITE_GUIDE.md`。
> 最近更新：2026-09-06（补齐推送/部署说明，更新现状）

---

## 一、发一篇新文章（3 步）

### 第 1 步：打开终端并进入网站目录

在网站目录下打开 PowerShell（地址栏输入 `powershell` 回车，或先 `cd` 过去）。

### 第 2 步：新建文章文件

```powershell
hexo new "我的第一篇博客"
```

> 如果提示 `hexo` 不是命令，改用：`npx hexo new "我的第一篇博客"`

执行后会在 `source\_posts\` 下生成 `我的第一篇博客.md`，用任何编辑器（记事本/VSCode/Obsidian）打开它，会看到：

```markdown
---
title: 我的第一篇博客
date: 2026-08-31 12:00:00
tags:
---

```

在 `---` 下面写正文（支持 Markdown：`#` 标题、`**加粗**`、图片、代码块等）。

> 💡 常用 front-matter 字段：
> - `tags: [标签1, 标签2]` 打标签
> - `categories: [学习笔记, 数据库导论]` 分类（两级分类这样写）
> - 网站已有「标签」「分类」页面，写了对 应字段文章会自动归类

### 第 3 步：本地预览确认效果（可选但推荐）

```powershell
hexo server
```

浏览器打开 http://localhost:4000 就能看到效果。确认没问题后，按 `Ctrl+C` 停止预览。

### 第 4 步：推送到 GitHub（= 发布上线）

```powershell
git add -A
git commit -m "新增文章：我的第一篇博客"
git push
```

> 本网站已接 **Netlify 自动部署**：push 成功后等 1~3 分钟，刷新 https://heaven-wash.top 即上线。
> 若看到旧内容，按 **Ctrl+F5** 强刷（Cloudflare 缓存）。
> 想验证是否推送成功：GitHub 仓库主页 → Commits。

**发布笔记时的特殊注意：**
- **图片**：正文引用的图片要手动复制到 `source\images\` 下（建议英文文件名），正文里写 `![说明](/images/文件名.png)`
- **Mermaid 图**：笔记里的 ```mermaid 代码块要改成 `{% mermaid %}...{% endmermaid %}` 标签才会渲染成图（本地 Obsidian 里的源笔记不用改）

---

## 二、编辑已有文章

直接用编辑器打开 `source\_posts\` 下对应的 `.md` 文件修改，然后重复"第 4 步"的构建 + 推送即可。

---

## 三、修改网站样式（主题）

本网站的样式由 **Butterfly 主题**控制，有两种改法：

### 方式 A：改主题配置（菜单、头像、首页设置等）

主题的完整"开关面板"是 `node_modules\hexo-theme-butterfly\_config.yml`（用编辑器打开，每一项都有注释说明）。

网站根目录的 **`_config.butterfly.yml` 是主题覆盖文件**（目前含：导航菜单、Mermaid 渲染、侧边栏 Follow 按钮链接）——想改主题设置，在这里写 `键名: 值` 即可（Hexo 自动合并，根目录优先，升级主题不丢失）。改完保存 → 重新构建：

```powershell
hexo clean
hexo generate
```

### 方式 B：自定义 CSS（改颜色、字体、布局细节）

进阶：在 `source\css\custom.css` 写样式，再通过主题配置的 `inject` 引入（升级主题不丢）。

### 修改站点标题、作者等

打开 `_config.yml`，改这几项（已配置好，一般不用动）：

```yaml
title: Heaven Washing Machine's BLOG
subtitle: 个人博客
author: 天鸡
language: zh-CN
url: https://heaven-wash.top
```

改完记得重新构建推送。

---

## 四、常见问题

| 问题 | 解决办法 |
| ---- | ---- |
| `hexo` 不是内部或外部命令 | 用 `npx hexo ...` 代替 |
| 预览时改了内容没变化 | 预览是实时刷新的；若不行按 Ctrl+C 重新 `hexo server` |
| 改了配置没效果 | 先 `hexo clean` 再 `hexo generate` |
| 端口 4000 被占用 | `hexo server -p 5000` 换个端口 |
| `git push` 提示 Permission denied | SSH key 未注册：把 `C:\Users\55871\.ssh\id_rsa.pub` 内容添加到 https://github.com/settings/ssh/new |
| 线上没更新 | push 后 Netlify 构建需 1~3 分钟；Ctrl+F5 强刷；还旧就去 app.netlify.com → Deploys 看日志 |
| Netlify 构建报 `hexo: Permission denied` | 仓库误提交了 node_modules → `git rm -r --cached node_modules` 后重新 push |
| 本地预览访客数是几千万大数字 | 正常现象：localhost 没有独立计数，显示的是不蒜子全局累计；线上域名是真实独立计数 |

---

## 五、网站现状（2026-09-06 更新）

- ✅ **站点信息**：作者 天鸡、副标题 个人博客、语言 zh-CN、url https://heaven-wash.top
- ✅ **部署链路**：GitHub → Netlify（自动构建，命令 `npm run netlify`）→ Cloudflare 域名；push 到 main 即上线
- ✅ **SSH**：本机 key 已注册 GitHub（经 ssh.github.com:443），直接 `git push` 即可
- ✅ **仓库卫生**：`.gitignore` 已建；误入库的 `public/`、`db.json`、`node_modules` 均已移出
- ✅ **导航与页面**：首页 / 归档 / 标签 / 分类，标签页与分类页已创建，Mermaid 渲染已开
- ✅ **内容**：模板测试文已删除；正式文章：831 数据库导论笔记
